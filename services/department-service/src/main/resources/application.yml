spring:
  application:
    name: department-service

  config:
    import: optional:configserver:http://localhost:8888

  cloud:
    config:
      enabled: true
      import-check:
        enabled: false
