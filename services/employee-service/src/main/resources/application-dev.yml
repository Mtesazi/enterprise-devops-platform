spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/employee_db_dev
    username: postgres
    password: postgres

logging:
  level:
    com.mtesazi: DEBUG
    org.springframework.web: DEBUG
    org.hibernate: DEBUG
